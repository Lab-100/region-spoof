# Region Spoof Applet

Апплет системного регион-спуфинга для Windows: подбирает **общедоступные прокси
выбранной страны** (через ProxyBroker2) и направляет трафик всех приложений
через них так, чтобы сервисы за рубежом видели исходящий адрес нужного региона.

> **Текущий статус: альфа-версия 1.2-alpha.**
> Программа бесплатна и предназначена **только для некоммерческого использования**.

## Возможности

- **Подбор прокси автоматически**: ProxyBroker2 находит и проверяет публичные
  прокси выбранной страны, держит локальный прокси `127.0.0.1:8888` с авторотацией.
- **Автовыбор порта**: при запуске сканируются порты (основной + запасные),
  выбираются два свободных; при неподъёме основного — авто-фолбэк на запасной
  с проверкой результата и предупреждением в окне.
- **Полное логирование**: файл `logs\applet.log` (с ротацией) хранит работу
  программы и действия пользователя (с маркером `[action]`).
- **Три способа маршрутизации** (переключение в конфигурации / окне / трее):
  - `system` — системный прокси Windows (Windows Proxy Settings) — работает без прав администратора;
  - `singbox` — TUN-туннель через sing-box (обязателен запуск от администратора);
  - `mihomo` — TUN-туннель через mihomo (обязателен запуск от администратора).
- **Рубильник зоны**: «Зарубежный регион» ↔ «РФ (локально)» — одним кликом.
- **Список регионов** для большинства ИИ-сервисов (США, Германия, Нидерланды, …).
- **Индикация**: цветная «лампочка» в окне и иконка в трее
  (серый — выкл., жёлтый — подключение, зелёный — регион подтверждён, красный — ошибка);
  в подсказке трея и окне показывается фактический регион и IP.
- **Кнопка «Проверить регион»** — принудительная проверка исходящего региона.
- **Автопереключение**: если активная связка не запустилась/упала, апплет
  переходит на следующую связку; последняя рабочая связка запоминается и
  включается при следующем запуске (если включён автостарт).
- **Журнал**: окно-журнал приложения и файлы логов.
- **Вкладка «Подбор прокси»**: видно, какие прокси подбираются (адрес, тип,
  результат проверки) и из каких источников они получены.

## Установка

### Способ 1. Инсталлятор (рекомендуется)

Скачайте `RegionSpoof-1.2-alpha-Setup.exe` со страницы
[Releases](/Lab-100/region-spoof/releases), запустите его и следуйте мастеру.
Инсталлятор:

- создаёт ярлык на рабочем столе и папку в меню «Пуск»;
- содержит **деинсталлятор** (добавляется в «Установка и удаление программ»
  Windows и в меню «Пуск» → «Удалить Region Spoof»);
- устанавливает приложение в `C:\Program Files\RegionSpoof`;
- **не требует предустановленного Python** — среда, библиотеки и утилиты
  упакованы вместе с приложением.

### Способ 2. Из исходников

1. Установите Python 3.10–3.12 с [python.org](https://www.python.org/downloads/),
   отметьте **«Add python.exe to PATH»**.
2. Запустите `install.bat` (ставит `proxybroker2`, `pystray`, `Pillow`, `requests`).
3. Убедитесь, что в папке `bin\` лежат бинарники (при необходимости скачайте сами):
   - `bin\singbox\...\sing-box.exe` — https://github.com/SagerNet/sing-box/releases
   - `bin\mihomo\...\mihomo.exe` — https://github.com/MetaCubeX/mihomo/releases
   - `bin\wintun\bin\amd64\wintun.dll` — https://www.wintun.net/
     (нужен только для TUN-связок `singbox`/`mihomo`; системный прокси обходится без него)
4. Запустите `run.bat`.

## Системные требования

| Параметр | Требование |
| --- | --- |
| Операционная система | Windows 10 (1809+) или Windows 11, **64-разрядная (x64)** |
| Права | Администратор — для установки и TUN-связок. Связка «системный прокси» работает и без прав администратора |
| Интернет | Нужен для поиска прокси и проверки региона |
| Место на диске | ~500 МБ после установки (сам инсталлятор ~230 МБ) |

## Приватность и безопасность

- Приложение **не содержит личных ключей и токенов** — регион достигается через
  бесплатные публичные прокси; платные аккаунты и ключи не используются.
- Рабочие данные (конфигурация, логи) хранятся **только локально**, в
  `%LOCALAPPDATA%\RegionSpoof`, и разработчику **не отправляются**. При первом
  запуске программа покажет об этом сообщение.
- ВАЖНО: публичные прокси **небезопасны** (видят ваш трафик). Не передавайте
  через них пароли, платёжные данные и личные документы.
- Для конфиденциальной работы рекомендуются собственный VPS или платный прокси:
  их адреса можно подставить в конфигурацию локального сервера вручную.

## Использование

- Кнопка **Старт** — поднять связку (подбор прокси + маршрутизация).
- Кнопка **Стоп** — всё выключить и вернуть настройки Windows.
- **Зарубежная зона (спуфинг)** — галочка-рубильник: снята = РФ (прямой трафик),
  установлена = выбранный регион.
- **Регион** — выбор страны для исходящего трафика.
- **Связка** — выбор способа маршрутизации.
- **Проверить регион** — показать фактический регион исходящего соединения.
- Сворачивание окна прячет его в трей; управление доступно и из меню трея.

Подключение считается успешным, когда найден хотя бы один рабочий публичный
прокси выбранной страны и проверка подтвердила её — тогда лампочка зелёная,
в трее и окне видно название региона и IP.

## Конфигурация

При установке через инсталлятор конфигурация живёт в `%LOCALAPPDATA%\RegionSpoof\config.json`;
при запуске из исходников — рядом со скриптом:

- `region` — выбранный регион (ISO-код) для зарубежной зоны;
- `regions` — список доступных регионов (код + русское название);
- `strategy` / `last_working` — активная / последняя рабочая связка;
- `zone` — `foreign` (зарубежье) или `ru` (РФ);
- `proxy_port` — локальный порт подбора прокси (по умолчанию 8888);
- `spare_ports` — список запасных портов (по умолчанию 8889–8892);
- `scan_ports` — полный список портов сканирования при запуске
  (если пусто/отсутствует — строится из `proxy_port` + `spare_ports`);
- `pb2_start_timeout_sec` — таймаут проверки подъёма ProxyBroker2 (фолбэк);
- `check_interval_sec` — период авто-проверки региона;
- `verify_timeout_sec` — таймаут одной проверки;
- `failover_threshold` — число неудачных проверок до «красной» лампочки;
- `auto_start` — включать последнюю рабочую связку при запуске приложения.

## Ограничения

- Публичные прокси ненадёжны и могут исчезать; регион подтверждается роботом-проверкой.
- TUN-связки (`singbox`, `mihomo`) требуют запуска от администратора и `wintun.dll`.
- Системный прокси (`system`) распространяется на приложения, уважающие
  настройки прокси Windows; не на всё (например, не на часть игр).
- Проверка региона использует внешние сервисы ipapi.co / ipinfo.io / ip-api.com.
- Некоторые сервисы (Google Maps, YouTube, Google Play и др.) требуют также
  регион учётной записи, а не только IP — спуфинг на них не гарантирует результат.

## Кредиты

Проект собран из свободных программ — огромное спасибо их авторам за работу:

| Проект | Назначение | Лицензия |
| --- | --- | --- |
| [Python](https://www.python.org/) | среда выполнения | PSF License |
| [ProxyBroker2](https://github.com/constverum/ProxyBroker) | поиск/проверка прокси | Apache-2.0 |
| [sing-box](https://github.com/SagerNet/sing-box) | прокси-ядро (TUN) | GPL-3.0 |
| [mihomo](https://github.com/MetaCubeX/mihomo) | прокси-ядро (TUN) | GPL-3.0 |
| [pystray](https://github.com/moses-palmer/pystray) | трей-иконка | MIT |
| [Pillow](https://python-pillow.org/) | графика/иконки | HPND |
| [requests](https://docs.python-requests.org/) | HTTP-клиент | Apache-2.0 |
| [aiohttp](https://github.com/aio-libs/aiohttp) | асинхронный HTTP | Apache-2.0 |
| [aiodns](https://github.com/saghul/aiodns) | асинхронный DNS | MIT |
| [PyInstaller](https://pyinstaller.org/) | сборка в exe | GPL-2.0+ (с исключениями) |
| [Inno Setup](https://jrsoftware.org/isinfo.php) | инсталлятор | Inno Setup License |
| [MaxMind GeoLite2](https://dev.maxmind.com/geoip/geolite2-free-geolocation-data/) | база стран по IP | CC BY-SA 4.0 |
| [Wintun](https://www.wintun.net/) | TUN-драйвер Windows (для TUN-связок) | MIT (Microsoft) |

Полный текст — в файле [LICENSE.txt](LICENSE.txt).

## Лицензия

Свободное использование возможно, пока проект остаётся некоммерческим.
Подробности — в [LICENSE.txt](LICENSE.txt).